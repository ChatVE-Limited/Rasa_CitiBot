name: Trigger auto deployment for rasacitibot

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/rasacitibot-AutoDeployTrigger-3c44de79-5a0f-4003-9929-bf757a4d3e38.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.RASACITIBOT_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.RASACITIBOT_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.RASACITIBOT_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: 
          registryUsername: ${{ secrets.RASACITIBOT_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.RASACITIBOT_REGISTRY_PASSWORD }}
          containerAppName: rasacitibot
          resourceGroup: citibot_group
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


